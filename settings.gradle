dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warning: this repository is going to shut down soon

        String storageUrl = System.env.FLUTTER_STORAGE_BASE_URL ?: "https://storage.flutter-io.cn"

        repositories {
            maven {
                url '/Users/huaweiyuan/AndroidStudioProjects/HwFlutterPractice/hwpratice_flutter/build/host/outputs/repo'
            }
            maven {
                url "$storageUrl/download.flutter.io"
            }
        }
    }
}
rootProject.name = "hwpractice_android"
include ':app'
apply from: 'config.gradle'
if (isFlutterModule) {
    setBinding(new Binding([gradle: this]))                                // new
    evaluate(new File(                                                     // new
            settingsDir.parentFile,                                              // new
            'hwpratice_flutter/.android/include_flutter.groovy'                         // new
    ))

    include ':flutter_project'
    project(':flutter_project').projectDir = new File('../hwpratice_flutter')
}

